#include <SoftwareSerial.h>
SoftwareSerial BTSerial(0, 1); // RX, TX
const int sensorPin = A0;  // دبوس قراءة الحساس 

// الجديدة LED دبابيس 
const int ledRed = 11;
const int ledYellow = 10;
const int ledGreen = 8;
int sensorValue = 0;

void setup() {
pinMode(ledRed, OUTPUT);
pinMode(ledYellow, OUTPUT);
pinMode(ledGreen, OUTPUT);

Serial.begin(9600);
BTSerial.begin(9600);
Serial.println("System Started...");
}
void loop() {
sensorValue = analogRead(sensorPin); // قراءة الحساس
Serial.print("Sensor Value: ");
Serial.println(sensorValue);
BTSerial.println(sensorValue);

// LEDs turned off
digitalWrite(ledGreen, LOW);
digitalWrite(ledYellow, LOW);
digitalWrite(ledRed, LOW);

// حسب مستوى الماء LEDS التحكم في 
if(sensorValue >=500 {        //خطر →- أحمر 
digitalWrite(ledRed, HIGH);
digitalWrite(ledGreen, LOW);
digitalWrite(ledYellow, LOW);
BTSerial.println("Alert: Danger!");
)
｝
else if(sensorValue >= 200) // تحذير <- أصفر
digitalWrite(ledYellow, HIGH);
digitalWrite(ledGreen, LOW);
digitalWrite(ledRed, LOW);
BTSerial.println("Warning: Early Alert");

}
else if(sensorValu < 200 {
digitalWrite(ledGreen, HIGH); //أمن → أخضر 
digitalWrite(ledGreen, LOW);
digitalWrite(ledRed, LOW);
BTSerial.println("Status: Safe");
}
else{
digitalWrite(ledGreen, LOW);
digitalWrite(ledRed, LOW);
digitalWrite(ledYellow, LOW);
}
delay (500);
}
